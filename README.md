# Experiments with tflite whisper generative models.

The transformers are installed using:
https://huggingface.co/docs/transformers/installation
git clone https://github.com/huggingface/transformers.git
cd transformers
pip install -e .
